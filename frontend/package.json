{
  "name": "echo-frontend",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/bundle-qrcode.mjs && tsc -p tsconfig.json && node scripts/sync-static.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
